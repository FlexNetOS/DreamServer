interface:
  display_name: "DreamServer"
  short_description: "Repo-specific patterns and workflows for DreamServer"
  default_prompt: "Use the DreamServer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true